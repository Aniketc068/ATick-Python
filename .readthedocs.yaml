# Read the Docs build configuration — connect this repo at https://readthedocs.org and the site
# builds automatically on every push. (See docs/hosting.md for other hosting options.)
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt
